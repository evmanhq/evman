<%
  breadcrumb_nav.add_item 'Team'
  breadcrumb_nav.add_item current_team.name, team_path(current_team)
  breadcrumb_nav.add_item 'Event Properties', event_properties_path
  breadcrumb_nav.add_item @event_property.name

%>

<%= render @event_property %>
