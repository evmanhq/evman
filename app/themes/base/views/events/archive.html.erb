<%
  breadcrumb_nav.add_item "Events", events_path
  breadcrumb_nav.add_item "Archive"
%>

<div class="row">
  <% year = nil %>
  <% @events.each do |event| %>
    <% if year != event.begins_at.year %>
      <% if year != nil %>
        </div></div></div>
      <% end %>
      <% year = event.begins_at.year %>
          <div class="col-xl-3 col-lg-4 col-md-6">
      <div class="card">
        <div class="card-header"><%= year %></div>
        <div class="card-block">
  <% end %>
  <p><%= link_to event.name, event %></p>
<% end %>
</div>
</div>
</div>
</div>